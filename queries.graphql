query Me {
  viewer {
    id
    name
    email
  }
}

query Issue($id: String!) {
  issue(id: $id) {
    id
    identifier
    title
    url
    branchName
    description
    state {
      name
      color
    }
  }
}
